package Chapter3

object Exercise7 extends App {

  println()
  println(""" | In the implementation of product given in the book,
              | foldRight is used as argument. For any function call
              | by value (not by name), the argument are evaluated
              | before the call. So the arguments will have to be
              | evaluated until the end of list.""".stripMargin)

}
